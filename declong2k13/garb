#include<cstdio>
using namespace std;
int main()
{
	int t,n,a,n1;
	long long int ans;
	scanf("%d",&t);
	while(t--)
	{
		scanf("%d",&n);
		n1=n;
		while(n--)
		{
			scanf("%d",&a);
		}
		n=n1;
		ans=(n*(n-1))/2;
		printf("%lld\n",ans);
	}
	return 0;
}
